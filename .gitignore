# Dependencies
node_modules/

# Build output
electron-app/dist/
dist/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Local secrets
.env
.env.local
*.pat
*-token.txt

# Dropbox conflicts
*conflicted*
*~*

# Claude Code memory (local only — never push)
.claude/

# Données privées (licences avec noms perso) — KV Upstash est la vraie source
license-server/data/keys.json

# Dropbox metadata
.dropbox
.dropbox.cache

# Vercel local env pulls
license-server/.env.local
license-server/.env.prod
license-server/.env*
# Scripts temporaires
license-server/_*.js

# Assets de design sources (gardés localement, pas dans le repo)
electron-app/build/Versions antérieures/
electron-app/build/ChatGPT Image*.png
electron-app/build/Pastille*.png
electron-app/build/Icone*.png
electron-app/build/Logo*.png
# Pastilles redimensionnées non utilisées (revert au design rond pleins)
pastille-*.png

# Notarisation Apple (cles privees, certificats - JAMAIS pousser)
notarisation/
